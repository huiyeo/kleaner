<Window x:Class="Kleaner.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="{Binding Title}" Height="720" Width="1120" MinHeight="600" MinWidth="920"
        WindowStartupLocation="CenterScreen" FontFamily="Microsoft YaHei UI"
        Background="#F8F8FA" TextElement.Foreground="#202126">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="48" CornerRadius="0" GlassFrameThickness="0"
                            ResizeBorderThickness="6" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="46" /><Setter Property="Height" Value="48" />
            <Setter Property="Foreground" Value="#5E6068" /><Setter Property="Background" Value="Transparent" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button">
                <Border x:Name="Bd" Background="{TemplateBinding Background}"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" /></Border>
                <ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bd" Property="Background" Value="#11000000" /></Trigger><Trigger Property="IsPressed" Value="True"><Setter TargetName="Bd" Property="Background" Value="#1C000000" /></Trigger></ControlTemplate.Triggers>
            </ControlTemplate></Setter.Value></Setter>
        </Style>
        <Style x:Key="TitleBarCloseButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Style.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#E81123" /><Setter Property="Foreground" Value="White" /></Trigger></Style.Triggers>
        </Style>
        <Style x:Key="RailButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
            <Setter Property="MinHeight" Value="44" /><Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="VerticalContentAlignment" Value="Center" /><Setter Property="Padding" Value="12,8" />
            <Setter Property="Margin" Value="0,2" /><Setter Property="Foreground" Value="#52545C" /><Setter Property="FontSize" Value="13" />
        </Style>
    </Window.Resources>
    <Window.Style><Style TargetType="Window"><Style.Triggers><Trigger Property="WindowState" Value="Maximized"><Setter Property="BorderThickness" Value="7" /></Trigger></Style.Triggers></Style></Window.Style>
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="48" /><RowDefinition Height="*" /><RowDefinition Height="36" /></Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="#FDFDFF" TextElement.Foreground="#26272D">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <Border Width="26" Height="26" CornerRadius="8" Background="#4F46E5"><materialDesign:PackIcon Kind="Broom" Width="16" Height="16" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" /></Border>
                <TextBlock Text="{Binding Title}" Margin="10,0,0,0" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold" Foreground="#26272D" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                <Button Style="{StaticResource TitleBarButtonStyle}" Click="MinimizeButton_Click"><materialDesign:PackIcon Kind="Minus" Width="16" Height="16" /></Button>
                <Button Style="{StaticResource TitleBarButtonStyle}" Click="MaximizeButton_Click"><materialDesign:PackIcon Kind="SquareOutline" Width="13" Height="13" /></Button>
                <Button Style="{StaticResource TitleBarCloseButtonStyle}" Click="CloseButton_Click"><materialDesign:PackIcon Kind="Close" Width="16" Height="16" /></Button>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions><ColumnDefinition Width="220" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="White" BorderBrush="#E9E9EF" BorderThickness="0,1,1,0">
                <DockPanel Margin="14,18">
                    <StackPanel DockPanel.Dock="Top">
                        <Button Command="{Binding ScanCommand}" Style="{StaticResource RailButtonStyle}"><StackPanel Orientation="Horizontal"><materialDesign:PackIcon Kind="Magnify" Width="19" Height="19" /><TextBlock Text="{Binding ScanText}" Margin="12,0,0,0" /></StackPanel></Button>
                        <Button Command="{Binding OpenToolboxCommand}" Style="{StaticResource RailButtonStyle}"><StackPanel Orientation="Horizontal"><materialDesign:PackIcon Kind="Wrench" Width="19" Height="19" /><TextBlock Text="{Binding ToolboxText}" Margin="12,0,0,0" /></StackPanel></Button>
                        <Button Command="{Binding OpenStartupCommand}" Style="{StaticResource RailButtonStyle}"><StackPanel Orientation="Horizontal"><materialDesign:PackIcon Kind="RocketLaunch" Width="19" Height="19" /><TextBlock Text="{Binding StartupText}" Margin="12,0,0,0" /></StackPanel></Button>
                        <Rectangle Height="1" Fill="#ECECF1" Margin="8,18" />
                        <Button Command="{Binding OpenQuarantineCommand}" Style="{StaticResource RailButtonStyle}"><StackPanel Orientation="Horizontal"><materialDesign:PackIcon Kind="ShieldLock" Width="19" Height="19" /><TextBlock Text="{Binding QuarantineText}" Margin="12,0,0,0" /></StackPanel></Button>
                        <Button Command="{Binding OpenHistoryCommand}" Style="{StaticResource RailButtonStyle}"><StackPanel Orientation="Horizontal"><materialDesign:PackIcon Kind="History" Width="19" Height="19" /><TextBlock Text="{Binding HistoryText}" Margin="12,0,0,0" /></StackPanel></Button>
                    </StackPanel>
                    <StackPanel DockPanel.Dock="Bottom">
                        <Rectangle Height="1" Fill="#ECECF1" Margin="8,8,8,12" />
                        <Button Command="{Binding OpenAdvancedCommand}" Style="{StaticResource RailButtonStyle}"><StackPanel Orientation="Horizontal"><materialDesign:PackIcon Kind="Cog" Width="19" Height="19" /><TextBlock Text="{Binding AdvancedText}" Margin="12,0,0,0" /></StackPanel></Button>
                        <Button Command="{Binding OpenSettingsCommand}" Style="{StaticResource RailButtonStyle}"><StackPanel Orientation="Horizontal"><materialDesign:PackIcon Kind="Tune" Width="19" Height="19" /><TextBlock Text="{Binding SettingsText}" Margin="12,0,0,0" /></StackPanel></Button>
                    </StackPanel>
                </DockPanel>
            </Border>
            <Grid Grid.Column="1" Margin="32,28,32,20">
                <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="0,0,0,22">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                    <StackPanel><TextBlock Text="{Binding Title}" FontSize="24" FontWeight="SemiBold" Foreground="#202126" /><TextBlock Text="{Binding StatusText}" FontSize="13" Foreground="#747682" Margin="0,7,0,0" /></StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Command="{Binding ScanCommand}" Style="{StaticResource KleanerSecondaryButton}"><StackPanel Orientation="Horizontal"><materialDesign:PackIcon Kind="Refresh" Width="18" Height="18" /><TextBlock Text="{Binding ScanText}" Margin="7,0,0,0" /></StackPanel></Button>
                        <Button Command="{Binding CancelScanCommand}" Style="{StaticResource KleanerSecondaryButton}"><StackPanel Orientation="Horizontal"><materialDesign:PackIcon Kind="CloseCircleOutline" Width="18" Height="18" /><TextBlock Text="{Binding CancelText}" Margin="7,0,0,0" /></StackPanel></Button>
                        <Button Command="{Binding CleanCommand}" Style="{StaticResource KleanerPrimaryButton}" Margin="8,0,8,0"><StackPanel Orientation="Horizontal"><materialDesign:PackIcon Kind="Broom" Width="18" Height="18" Foreground="White" /><TextBlock Text="{Binding CleanText}" Foreground="White" Margin="8,0,0,0" /></StackPanel></Button>
                    </StackPanel>
                </Grid>
                <Border Grid.Row="1" Background="White" CornerRadius="12" BorderBrush="#E9E9EF" BorderThickness="1">
                    <DataGrid x:Name="RulesGrid" AutoGenerateColumns="False" CanUserAddRows="False" ItemsSource="{Binding Rows}" SelectionMode="Single" SelectedItem="{Binding SelectedRow}" GridLinesVisibility="None" HeadersVisibility="Column" RowHeight="46" BorderThickness="0" Background="Transparent">
                        <DataGrid.ColumnHeaderStyle><Style TargetType="DataGridColumnHeader"><Setter Property="Background" Value="#FAFAFC" /><Setter Property="Foreground" Value="#777984" /><Setter Property="FontSize" Value="12" /><Setter Property="FontWeight" Value="SemiBold" /><Setter Property="Padding" Value="12,12" /><Setter Property="BorderBrush" Value="#ECECF1" /><Setter Property="BorderThickness" Value="0,0,0,1" /></Style></DataGrid.ColumnHeaderStyle>
                        <DataGrid.CellStyle><Style TargetType="DataGridCell"><Setter Property="Padding" Value="12,0" /><Setter Property="BorderThickness" Value="0" /><Setter Property="VerticalContentAlignment" Value="Center" /></Style></DataGrid.CellStyle>
                        <DataGrid.RowStyle><Style TargetType="DataGridRow"><Setter Property="Background" Value="Transparent" /><Setter Property="BorderBrush" Value="#F0F0F3" /><Setter Property="BorderThickness" Value="0,0,0,1" /><Style.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#F7F7FF" /></Trigger><Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="#EEEEFF" /></Trigger></Style.Triggers></Style></DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Width="48" />
                            <DataGridTextColumn Binding="{Binding Name}" IsReadOnly="True" Width="220" />
                            <DataGridTextColumn Binding="{Binding CategoryDisplay}" IsReadOnly="True" Width="110" />
                            <DataGridTextColumn Binding="{Binding FileCount}" IsReadOnly="True" Width="72" />
                            <DataGridTextColumn Binding="{Binding SizeDisplay}" IsReadOnly="True" Width="96" />
                            <DataGridTextColumn Binding="{Binding RiskDisplay}" IsReadOnly="True" Width="56" />
                            <DataGridTextColumn Binding="{Binding ElevatedDisplay}" IsReadOnly="True" Width="64" />
                            <DataGridTextColumn Binding="{Binding Note}" IsReadOnly="True" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
                <Border Grid.Row="2" Background="#FFFFFF" CornerRadius="10" BorderBrush="#E9E9EF" BorderThickness="1" Margin="0,16,0,0" Padding="16,12">
                    <Grid><Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal"><materialDesign:PackIcon Kind="ShieldCheckOutline" Width="18" Height="18" Foreground="#4F46E5" /><TextBlock Text="{Binding SafetyHeader}" FontWeight="SemiBold" Margin="8,0,0,0" /></StackPanel>
                        <TextBlock Grid.Row="1" Text="{Binding SafetyNotesText}" TextWrapping="Wrap" MaxHeight="72" TextTrimming="CharacterEllipsis" Foreground="#666873" Margin="26,8,0,0" />
                    </Grid>
                </Border>
            </Grid>
        </Grid>
        <Border Grid.Row="2" Background="#FDFDFF" BorderBrush="#E9E9EF" BorderThickness="0,1,0,0"><TextBlock Text="{Binding StatusText}" Margin="22,0" VerticalAlignment="Center" FontSize="12" Foreground="#7A7C87" /></Border>
    </Grid>
</Window>
