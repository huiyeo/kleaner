# 10: 隔离区事务隔离与强制审计

**What to build:** 清理、还原与隔离区清空目前允许省略 `HistoryManager`；`RestoreBatch` 对逐项失败静默吞掉后仍删除整个批次，可能永久删除未还原文件；批次 ID 只有秒级精度，manifest 在移动后一次性写入。将所有隔离区状态变化收束为带强制审计的事务：审计或事务初始化失败时不得移动文件；逐项状态和 manifest 必须原子落盘；还原只能在全部条目成功后移除批次；永久清空必须准确报告并审计失败。

**Blocked by:** None。所有自动化用例仅使用注入的临时隔离区与临时 history 路径；不得触发真实用户目录、真实隔离区、注册表或系统清理。

**Status:** ready-for-agent

- [x] `QuarantineManager` 强制依赖可写 `HistoryManager`；所有 App、CLI 与测试调用点显式提供历史对象（测试使用临时路径）
- [x] 移入在首次文件状态变化前建立唯一批次、原子 manifest 和审计起始记录；初始化失败拒绝执行
- [ ] manifest 记录逐项状态，移动成功后原子更新；异常与部分成功可恢复、可审计（常规路径已实现，中断与持久化故障恢复仍待验证）
- [x] `RestoreBatch` 对任何未恢复或失败条目保留批次及其文件，绝不调用递归删除；向调用方报告 restored/skipped/failed
- [x] `DeleteBatch` / `PurgeOlderThan` 在永久删除前后准确记录结果；删除失败不伪报成功
- [x] xunit 覆盖审计不可写拒绝执行、批次 ID 唯一、manifest 原子状态、部分还原保留、永久清空失败与审计
- [x] 同步 `docs/deletion-path.md`、`docs/architecture.md`、`docs/context.md`、README 与 UI 文案
- [x] Release 构建 0 警告/0 错误，完整测试绿色

**验收记录（2026-09-05）：** Release 构建 0 警告/0 错误，xunit 79/79 通过。全部测试使用临时目录、临时 history 文件与受控文件锁；未操作真实隔离区或用户文件。视觉走查沿用工单 09 的限制：当前自动化环境无法访问原生窗口，发布前仍需人工检查隔离区窗口的成功、部分还原和清空失败提示。

## Comments

操作互斥：四个受控重入回归在修复前失败；Execute/RestoreBatch/DeleteBatch/PurgeOlderThan 现持有根目录 `.operation.lock` 独占句柄，竞争请求在状态读取/审计/移动之前拒绝。锁文件保留，避免删除重建导致双持有；Purge 内部使用已持锁核心路径。增加独立进程持锁后退出场景，验证跨进程拒绝与退出后恢复；完整测试 126/126、Release 零警告零错误。外部路径竞争、嵌套或别名根目录、最终审计补记仍需后续处理。

日志读取边界：两个失败回归复现超长行被接受与 null 行导致展示排序崩溃。修复为 4096 字符分块、65536 字符单行上限、审计拒绝/展示跳过；Recent 仅保留最新 K 项（默认 200、最多 1000）。额外覆盖跨缓冲 CRLF、无末尾换行、超限写入、无限输入有限读取拒绝及旧排序一致性。完整测试 121/121、Release 零警告零错误。成本样本及局限见 docs/performance-history-probe.md，不能视为全产品性能基线或事务完成。

审计重试去重：增强还原清单故障用例，旧实现两个文件产生三条 restore-file 记录，失败复现后引入固定标识 AppendOnce。核对与追加使用同一个排他写句柄，相同内容不追加，冲突/损坏不覆盖；增加六项历史测试，并增强独立进程恢复记录数断言。完整测试 113/113、Release 零警告零错误。旧随机 ID 记录不自动迁移，最终汇总补记及历史展示未完成；当前逐项全日志扫描存在大批量 IO 成本，须进入本工单后续性能核查，不能作为最终性能方案。

逐项还原审计：临时 history 文件在还原意图写入时被锁定，旧实现继续移动并删除整个批次，回归断言失败。现移动/核对后先追加并刷新 restore-file，再移除恢复意图；审计失败停止，重试恢复可写后核对并补记。另覆盖全部文件还原后最终汇总被锁定，仍保留含固定目标和摘要的逐项证据。Release 零警告零错误，完整测试 107/107。最终汇总补记、至少一次记录的去重、中文历史展示及其他操作的审计恢复仍未收口。

本轮验收：Release 构建零警告零错误，完整测试 105/105（含两项独立进程中断恢复）。未操作真实用户文件、真实隔离区或系统设置。

独立进程验证：新增仅测试使用的 CrashWorker，在清理移动后或还原移动后、临时清单刷新但尚未替换时直接退出（不执行 finally）。两项回归通过：父进程读取最后有效 pending/restoring 清单，核对并恢复全部夹具内容；中断操作只有起始审计，没有伪造的成功记录。遗留临时清单原样保留，因此恢复报告及审计为 partial，空条目批次仍在；尚未实现自动收尾，不把“文件恢复”写成“事务完整完成”。断电、最终审计故障及并发竞争仍待验证。

还原意图进展：移动前记录 restoring、固定 RestoreTarget 和 SHA256；恢复重试核对内容，已还原项可以完成清单更新，同长度篡改保留失败条目，同名冲突沿用已记录目标不覆盖新文件。加强的重启恢复断言在旧实现失败；新增 5 个意图失败/冲突/篡改回归后完整测试 103/103、Release 零警告零错误。本次使用新管理器实例及原子替换故障注入，未声称已完成独立进程终止/断电验收。最终审计失败、并发路径替换仍待处理。

持久化故障进展：增加仅测试程序集可用的原子替换前故障注入点，实际序列化、临时文件写入、Flush 和替换仍走生产路径。旧实现在预写、移动后落盘及还原落盘故障时继续移动，三项回归复现后修复；初始清单失败、审计在初始化后被锁定也验证不移动文件。重建管理器可读取最后有效清单并恢复剩余文件。Release 零警告零错误，完整测试 98/98。尚未证明真实进程终止/断电、已恢复条目的身份核对和最终审计写入失败恢复，因此工单仍打开。

重新打开：原测试只断言清空失败时目录还在，未证明 manifest 仍可读取及剩余文件可还原。当前清空枚举会删除 manifest；还原收尾会删除根目录所有文件，并在收尾之前记录 ok。

- [x] 清空遇到锁定文件时保留 manifest，重建管理器后仍可列出批次并恢复剩余文件。
- [x] 还原收尾保留未登记文件（含嵌套文件），报告及审计均为 partial。
- [x] 全部成功时仍能移除空批次；保留原生界面验收及其他事务故障覆盖的未完成状态。

本次证据：增强的锁定清空用例及根目录/嵌套未知文件用例，在修复前均失败（3/3），修复后通过；完整 Release 构建 0 警告/0 错误，84/84 测试通过。测试仅使用临时目录和注入的 history 路径。

后续仍需覆盖：清单写入失败后的停止与重启恢复、移动后审计失败、还原中断、批次路径及清单条目的约束。原 79 项测试通过不能证明整张事务工单完成，因此保留 ready-for-agent。

路径校验进展：四个篡改场景（批次外路径、相似前缀兄弟目录、错误映射、错误批次 ID）在旧代码上失败，修复后通过。整批先验证，后续条目损坏也不会移动前面的合法项。Release 零警告零错误，完整测试 88/88；加强整批原子拒绝断言后定向测试 4/4。目录联结、并发替换、清单真实性和中断恢复仍未完成，本票继续打开。

目录联结进展：真实 Windows junction 分别置于批次、来源祖先和目标祖先，三个回归在旧实现上失败，修复后通过；另补隔离区初始化路径含 junction、管理器创建后根目录替换的拒绝测试。链接与目标均在每个测试的 GUID 临时目录中，退出时先移除链接本身。完整测试 93/93，Release 零警告零错误。现存 reparse point 检查已接入移动与清单写回路径；检查与 IO 之间的竞争窗口、清单真实性及事务中断仍需后续处理。
