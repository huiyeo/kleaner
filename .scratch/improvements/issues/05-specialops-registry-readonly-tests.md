# 05: SpecialOps 注册表只读扫描注入化 + 测试

**What to build:** `Kleaner.SpecialOps`（164 行）目前零专测，其中注册表卸载残留只读扫描是对用户的"只扫描不删除"承诺载体。参照工单 03 的模式做前置小重构：注册表访问改为可注入（缺省仍走真实实现，现有调用点无参构造保持兼容），随后补测试。

**Blocked by:** None（可立即开始）

**Status:** complete

- [x] 注入的假注册表实现上断言：扫描过程只调用读取类 API，零写入/删除调用
- [x] 指向不存在路径的卸载条目被识别为残留候选；路径仍存在的条目不进入结果
- [x] 扫描异常（键不可读）被吞并记录而非中断整个扫描
- [x] HKLM/提权路径（reg.exe runas）不纳入单元测试，测试中注明排除原因
- [x] WSL vhdx 检测与系统大件清单中可纯化的纯逻辑（路径拼接/大小格式化）顺带覆盖
- [x] 测试全程不触碰真实注册表，CI 可安全运行

**完成说明：** `IReadOnlyRegistryReader` 仅暴露打开、枚举与取值操作；生产默认实现仍以 `writable: false` 打开 Windows 注册表。4 例测试使用假实现与路径存在性委托，不访问真实注册表，并锁定三条生产默认根路径。HKLM 的 `reg.exe runas` 属于 `StartupManager` 的写入路径，不是本只读扫描的职责，按工单边界排除。
