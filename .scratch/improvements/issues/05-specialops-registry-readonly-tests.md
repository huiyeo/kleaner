# 05: SpecialOps 注册表只读扫描注入化 + 测试

**What to build:** `Kleaner.SpecialOps`（164 行）目前零专测，其中注册表卸载残留只读扫描是对用户的"只扫描不删除"承诺载体。参照工单 03 的模式做前置小重构：注册表访问改为可注入（缺省仍走真实实现，现有调用点无参构造保持兼容），随后补测试。

**Blocked by:** None（可立即开始）

**Status:** ready-for-agent

- [ ] 注入的假注册表实现上断言：扫描过程只调用读取类 API，零写入/删除调用
- [ ] 指向不存在路径的卸载条目被识别为残留候选；路径仍存在的条目不进入结果
- [ ] 扫描异常（键不可读）被吞并记录而非中断整个扫描
- [ ] HKLM/提权路径（reg.exe runas）不纳入单元测试，测试中注明排除原因
- [ ] WSL vhdx 检测与系统大件清单中可纯化的纯逻辑（路径拼接/大小格式化）顺带覆盖
- [ ] 测试全程不触碰真实注册表，CI 可安全运行
