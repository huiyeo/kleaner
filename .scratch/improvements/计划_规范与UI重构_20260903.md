# 计划_规范与UI重构_20260903

> 三任务执行计划，经拷问对齐后锁定。顺序有依赖：阶段 0 → 1 → 2 → 3，不可并行领取相邻阶段。阶段 3 产出工单后由人工派发。

## 已锁定的决策

| 决策点 | 结论 |
|---|---|
| 任务顺序 | 语言规范先定 → UI 重构收尾提交 → 规范全量落地 → 优化盘点 |
| UI"完成"标准 | 统一档：收尾当前 8 文件改动 + 视觉走查 + 样式上收（7 个工具窗口零私有样式；MainWindow 标题栏专属样式保留窗口内并注释标明） |
| 未提交改动处置 | 先静态审查样式 Key → Release 构建 → 逐窗口截图走查 → 修复后一次性提交 |
| 项目语言范围 | 提交信息规范 + UI 文案走 S 通道 + .editorconfig/分析器/注释语言；文档语言暂不约定 |
| 提交信息语言 | 英文 type/scope + 中文 subject（conventional commits） |
| 注释语言 | 统一中文 |
| 分析器严格度 | 警告级，不设 TreatWarningsAsErrors，存量清单化观察 |
| 优化盘点维度 | 架构（模块边界/依赖方向）+ 测试（Executor/SpecialOps/App VM 覆盖缺口） |
| 产出形式 | 盘点报告 → 人工勾选 → 工单落 `.scratch/improvements/issues/`（沿用三张工单格式） |

## 阶段 0 — 规范固化 【已完成 2026-09-03】

- [x] `docs/conventions.md`（提交信息/注释/文案/分析器四节）
- [x] `.editorconfig`（警告级基线）
- [x] `docs/analyzer-baseline.md` 占位
- [x] AGENTS.md 增加规范引用
- [x] 本计划落盘

## 阶段 1 — UI 重构收尾 【已完成 2026-09-03】

- [x] 静态审查：样式 Key 引用一致性——无缺失引用；`AdvancedWindow.BigItemTemplate` 为代码后置 `FindResource` 合法引用
- [x] Release 构建验证（0 错误）+ 60/60 测试通过
- [x] 视觉走查：7 个窗口全部截图核对（工具箱/高级模式含全部标签页）
- [x] 修复：主窗口主按钮文字裁切（私有 `PrimaryActionStyle` 与 MDIX 模板冲突，收敛到共享 `KleanerPrimaryButton`）；历史窗口时间列截断秒位（150→175）
- [x] 按规范提交

**走查遗留（转阶段 2 / 3）**：`AdvancedWindow` 注册表页自动生成列头 "Path" 为英文属性名；最小尺寸（920×600）走查因自动化窗口拖拽不可用未覆盖，建议人工抽查一次。

**验收**：走查通过、构建 + 60 测试绿、单次规范提交。

## 阶段 2 — 规范全量落地 【已完成 2026-09-03】

- [x] 文案审计：C# 侧唯一硬编码为 `AdvancedWindow.xaml.cs:18` 列头 `"Path"` → 改 `S.Get("ColPath")` 并补词条；XAML 侧 6 个窗口 `Title` 回退值与工具箱 3 个 `TabItem Header` 为死值（构造函数已用 S 覆盖），替换为 S key 名消除硬编码
- [x] 注释语言核查：全仓源码注释统一中文，英文注释仅存在于 obj/ 生成物；`RuleRow.cs:18` 的 `"本机实测"` 为规则库数据域比较值，非 UI 文案，豁免
- [x] 分析器存量告警盘点：3 个 xUnit2008（测试层），记入 `docs/analyzer-baseline.md`；`S.Load` 静默回退评估结论为保持现状

## 阶段 3 — 优化盘点（只盘点不修改）【待开始】

- [ ] 架构维度：五模块依赖方向与边界（Core 无 UI 依赖、Executor/SpecialOps 职责边界、App MVVM 纯度）
- [ ] 测试维度：Executor / SpecialOps / App ViewModel 覆盖缺口与可测性改造点（参照工单 03 的可注入模式）
- [ ] 报告交付 → 人工勾选 → 工单落 `.scratch/improvements/issues/`

**风险与上报**：盘点若发现高危安全语义问题，不等工单流程，即时单独上报。
