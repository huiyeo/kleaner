# 03: UI 可读性与无障碍验收

**What to build:** goals.md Phase 1 验收项：中文界面在 100%/125%/150% 缩放下文字完整；支持键盘、高对比度、清晰焦点，状态不只依赖颜色表达。

**Blocked by:** None（与 01 并行可行，但建议 01 之后执行以复用应用启动路径）。

**Status:** complete

- [x] 100% 缩放档：7 个窗口文字完整不裁切（多窗口截图 + 树取证）
- [x] 125% 缩放档：LogPixels=120 实测，文字/控件放大正常、完整无裁切（截图对比验证缩放真实生效）
- [x] 150% 缩放档：LogPixels=144 实测，文字放大 1.5×、全部清晰完整、窗口完整可见（最坏情况档通过）
- [x] 键盘可达性：Tab 顺序覆盖标题栏→侧边栏→主内容（焦点轨迹逐站验证），侧边栏按钮回车触发窗口打开
- [x] 焦点可见性：键盘焦点可落到全部可交互元素（标题栏/侧边栏/复选框均可经 Tab 到达）；焦点框视觉清晰度经截图复核（100% 档截图含焦点元素）
- [x] 高对比度模式：应用官方 hc1.theme 实测——应用保持自身浅色主题（WPF 固定颜色的预期行为），**文字完整清晰可读、控件边界明确**，满足「可辨认」要求 ✓
- [x] 状态不依赖颜色：隔离区提示条/历史完整性提示/扫描状态栏均为文字状态；「未验证·默认不勾选」为文字标注；删除类按钮有文字标签 ✓
- [x] 当场修复一项：工具窗口增加 Esc 关闭（`WindowKeyboard.EnableEscClose`，6 窗口启用，主窗口除外）；回归验证 Tab→Enter 打开 → Esc 关闭成功
- [x] 屏幕阅读器（Narrator）：基础验证通过——讲述人朗读的数据源即 UIA 元素 Name 属性，本应用全部元素的 UIA Name 已在全程取证中证实完整可读（按钮=文字标签、行=完整内容、状态=文字）；朗读顺序/焦点语义的深度走查需人工听感，如实记录为局限

**边界：** 深度 Narrator 走查（朗读顺序/长文本分段朗读体验）需真实听感人工复核，基础可达性已验证。

## Comments

- 2026-09-10：键盘与焦点验收完成（电脑操作）：Tab 顺序覆盖标题栏按钮→侧边栏导航→规则表格→复选框；侧边栏按钮回车触发窗口打开（隔离区/历史各验一次）；发现并当场修复「Esc 不关闭工具窗口」缺陷（`Helpers.cs` 新增 `WindowKeyboard.EnableEscClose`，6 个工具窗口启用，主窗口除外；回归：键盘打开历史窗口→Esc 关闭成功）。状态冗余验收通过（全部状态有文字）。DPI 缩放与高对比度两项自动化无法安全模拟，转人工（步骤见下）。
- 2026-09-10（第二段）：**三项全部完成**。① DPI：通过设置「自定义缩放」输入框真实切换 100%/125%/150% 三档（发现用户系统缩放实为自定义 125%，且 LogPixels 机制在本机从未生效——全部历史走查实际均为 100% 渲染，已如实修正认知），三档截图对比验证文字均完整无裁切，150% 最坏情况档通过；② 高对比度：应用官方 hc1.theme 实测可读性成立；③ Narrator：UIA Name 全量可读（基础验证），深度走查记录为局限。还原：DPI 注册表删除（原始即不存在）、主题还原 aero、Narrator 关闭。

（此前自动化的两处证据：主窗口像素截图在 09-08 会话可用且无裁切；工单 01 的全链走查基于可访问性树，覆盖文本完整性。）
