# 03: UI 可读性与无障碍验收

**What to build:** goals.md Phase 1 验收项：中文界面在 100%/125%/150% 缩放下文字完整；支持键盘、高对比度、清晰焦点，状态不只依赖颜色表达。

**Blocked by:** None（与 01 并行可行，但建议 01 之后执行以复用应用启动路径）。

**Status:** ready-for-human

- [x] 100%（当前缩放档）下 7 个窗口文字完整不裁切（电脑操作逐窗口树取证 + 截图；此前 Phase 0 各窗口走查亦无裁切）
- [x] 键盘可达性：Tab 顺序覆盖标题栏→侧边栏→主内容（焦点轨迹逐站验证：关闭→重新扫描→工具箱→启动项→隔离区/历史），侧边栏按钮回车触发隔离区/历史窗口打开
- [x] 焦点可见性：键盘焦点可落到所有可交互元素（标题栏/侧边栏/复选框均可经 Tab 到达）——焦点框视觉清晰度需像素级人工复核（本环境像素捕获不稳定）
- [x] 状态不依赖颜色：隔离区提示条/历史完整性提示/扫描状态栏均为文字状态；「未验证·默认不勾选」为文字标注；删除类按钮有文字标签 ✓
- [x] 当场修复一项：工具窗口（隔离区/历史/设置/工具箱/高级/启动项）增加 Esc 关闭（`WindowKeyboard.EnableEscClose`，主窗口除外）；回归验证 Tab→Enter 打开历史窗口 → Esc 关闭成功
- [ ] 125%/150% 缩放文字完整性：自动化环境无法安全模拟（LogPixels 注册表法在 Win11 不生效，系统缩放需显示设置手动切换且影响全桌面）——**待人工验收**：设置→系统→屏幕→缩放 125%/150% 下走查各窗口
- [ ] 高对比度模式：需切换系统主题人工验收（应用使用固定浅色主题，高对比度下表现未验证）——**待人工验收**
- [ ] 屏幕阅读器（Narrator）：自动化环境无法可靠评估——**待人工验收**

**边界：** 自动化可验证部分已完成并记录；DPI 缩放/高对比度/Narrator 三项需要人工在真实显示环境执行（各约 10 分钟），已在下方给出操作步骤。

## Comments

- 2026-09-10：键盘与焦点验收完成（电脑操作）：Tab 顺序覆盖标题栏按钮→侧边栏导航→规则表格→复选框；侧边栏按钮回车触发窗口打开（隔离区/历史各验一次）；发现并当场修复「Esc 不关闭工具窗口」缺陷（`Helpers.cs` 新增 `WindowKeyboard.EnableEscClose`，6 个工具窗口启用，主窗口除外；回归：键盘打开历史窗口→Esc 关闭成功）。状态冗余验收通过（全部状态有文字）。DPI 缩放与高对比度两项自动化无法安全模拟，转人工（步骤见下）。
- **人工验收步骤**：① 设置→系统→屏幕→缩放分别设 125%/150%，运行应用逐窗口检查文字是否裁切；② 设置→辅助功能→对比度主题启用后检查应用可读性；③ 可选：Narrator 走查。完成后在本票勾选对应项并置 Status: complete。

（此前自动化的两处证据：主窗口像素截图在 09-08 会话可用且无裁切；工单 01 的全链走查基于可访问性树，覆盖文本完整性。）
