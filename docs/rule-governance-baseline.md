# 规则库治理基线（首份，2026-09-10）

> 工单 rule-governance 05。测量工具：`governance-report`（口径见 `docs/rule-governance.md`）。
> 本报告是 Phase 2 指标目标值决策（工单 03）的锚点数据，不是结论。

## 总量指标

| 指标 | 值 |
|---|---|
| 规则总数 | 101 |
| 证据覆盖率 | **100%**（101/101：safetyNotes ≥20 字 + safetyDoc 锚点） |
| 验证覆盖率 | **23.8%**（24/101 「本机实测」= 默认勾选集） |
| 分类覆盖 | 6/6（temp/browser-cache/dev-cache/updater/system/application） |
| 唯一目标根 | 5（%TEMP% %LOCALAPPDATA% %APPDATA% %WINDIR% 等） |
| 默认勾选规则数 | 24（= 验证通过集，与产品语义一致） |

## 未验证规则分布（77 条，默认不勾选）

| 分类 | 数量 | 占比 |
|---|---|---|
| application（应用缓存/数据） | 34 | 44% |
| dev-cache（开发工具缓存） | 23 | 30% |
| browser-cache（浏览器缓存） | 12 | 16% |
| updater（更新器安装包） | 4 | 5% |
| system（系统项） | 3 | 4% |
| temp（临时目录） | 1 | 1% |

`verified` 值分布仅两种规范形态：`本机实测 2026-08-29（扫描命中…）`×24、`官方文档来源，本机未验证，默认不勾选`×77——**数据卫生良好，无第三态脏值**。

## 评估

- **证据覆盖率满分**：Phase 0 工单 11 的 fail-closed 语义（不足 20 字即校验失败）已把证据质量锁死在 100%。
- **验证覆盖率 23.8% 是保守策略的体现而非缺陷**：未验证规则默认不勾选 → 对普通用户零误伤暴露；提升该指标的唯一途径是贡献者在对应软件环境真机实测（CONTRIBUTING 三关流程），属于社区/所有者长期动作，不是本代码库可自动完成的。
- **无撤回候选**：全部 101 条规则无危险外露（未验证者不可达）；暂无需启动撤回机制（机制本身由 rule-governance 04 建设，供未来使用）。

## 后续动作建议（不阻塞）

1. 24 条本机实测规则建议定期复测（证据新鲜度——rule-governance 04 的 evidence 时间戳机制落地后的自然延伸）。
2. 77 条未验证规则按分类逐个真机实测可作为一个持续的社区贡献方向；每转正一条，验证覆盖率 +1pt。
