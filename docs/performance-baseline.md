# 性能基线（工单 15）

本文件记录可复现的性能测量结果与 SLO 候选。**当前没有任何指标被冻结**：以下数据来自单机单次全套运行的 5 轮样本，仅用于建立基线与暴露量级。冻结条件见文末。

## 复现方式

```powershell
pwsh ./scripts/bench.ps1 -Files 2000 -Iterations 5 -ColdStarts 5
```

脚本依次执行：Release 构建 → `gen-dataset` 生成合成数据集 → `bench` 五场景测量（每场景独立子进程，采集峰值工作集与 CPU 时间）→ 冷启动 5 次（进程启动到主窗口句柄出现）→ 汇总写入 `docs/performance/performance-run-<时间戳>.json`。

## 测量口径

| 项目 | 计时起止点 | 缓存定义 |
|---|---|---|
| rules-scan | `ScanEngine.Scan` 单次完整调用（含规则加载与逐规则枚举），Stopwatch 覆盖 | 热缓存（数据集刚生成、前一迭代已读入） |
| usage | `DiskUsageAnalyzer.TopLevel` 完整枚举 | 热缓存 |
| large-files | `LargeFileScanner.Scan`（阈值 64KB，top 100） | 热缓存 |
| duplicates | `DuplicateFinder.Find`（阈值 64B） | 热缓存 |
| cancel | 首个进度回调时延（扫描开始 → 首个 `ScanProgress`）；取消时延（`Cancel()` → `Scan` 返回，含 `OperationCanceledException` 结束） | 热缓存 |
| 冷启动 | `Process.Start` → 主窗口句柄首次非零 | 首次为冷文件缓存，其余为热缓存（脚本顺序执行） |

百分位取最近邻秩（升序第 ⌈p/100·n⌉ 个）。峰值内存为子进程自报的 `PeakWorkingSet64`。

## 本机数据集定义

参数（`gen-dataset --seed 20260906 --files 2000 --depth 3 --branching 4 --dup-groups 20`）：

- 2000 个文件，共 595.6 MB；目录树 3 层 × 每层 4 分支（84 个目录）
- 大小分布：70% 小（64B–16KB）、25% 中（16KB–128KB）、5% 大（128KB–512KB）
- 20 组重复文件（每组 3 副本，逐字节相同），内容随机填充
- 生成写入系统临时目录的 GUID 目录，测后清理；同种子同参数逐字节可复现

## 原始数据（2026-09-06 运行）

环境：Windows 11 专业版 build 26100，12th Gen Intel Core i5-12500H（12 核），.NET SDK 10.0.400，Release 构建。完整 JSON：`performance-run-20260906-122105.json`。

| 场景 | 样本（ms，5 轮升序） | p50 (ms) | p95 (ms) | 峰值内存 (MB) | CPU 总耗时 (ms) |
|---|---|---|---|---|---|
| rules-scan | 3022, 3035, 3078, 3135, 3420 | 3077.7 | 3420.1 | 60.6 | 16046.9 |
| usage | 58, 64, 77, 78, 78 | 76.7 | 78.3 | 32.8 | 421.9 |
| large-files | 60, 60, 77, 91, 112 | 76.9 | 112.1 | 35.3 | 468.8 |
| duplicates | 96, 104, 106, 114, 124 | 106.3 | 123.8 | 47.8 | 609.4 |
| cancel（总耗时） | 699, 744, 755, 787, 827 | 755.0 | 826.8 | 65.0 | 4046.9 |
| cancel（首进度时延） | 697.3, 718.2, 753.5, 783.9, 823.7 | — | — | — | — |
| cancel（取消时延） | 0.47, 0.49, 0.50, 0.56, 2.62 | — | — | — | — |
| 冷启动 | 1776, 927, 948, 990, 996 | — | 1776（首启冷缓存） | — | — |

## 跨日复跑（2026-09-08 运行）

同机、SDK 10.0.400、Release 构建。**注意：数据集为 1000 文件 / 294.9 MB（`-Files 1000 -Iterations 3 -ColdStarts 3`），与 09-06 的 2000 文件不同，绝对值不可直接对比；复跑目的是确认同量级可复现性，而非精确对照。** 完整 JSON：`performance-run-20260908-125602.json`。

| 场景 | 样本（ms，3 轮升序） | p50 (ms) | p95 (ms) | 峰值内存 (MB) |
|---|---|---|---|---|
| rules-scan | — | 2938.2 | 3513.0 | 59.1 |
| usage | — | 37.4 | 40.2 | 28.2 |
| large-files | — | 32.1 | 43.0 | 28.8 |
| duplicates | — | 53.1 | 69.6 | 38.4 |
| cancel | — | 540.6 | 618.9 | 60.1 |
| 冷启动 | 2876, 951, 1007 | — | 2876（首启冷缓存） | — |

**复跑结论：同量级可复现。** 1000 文件数据集的各场景耗时约为 2000 文件版本的一半（线性于数据规模，符合预期）；峰值内存同量级；取消时延与首进度同量级（540/619ms 与 09-06 的 755/827ms 相比随数据集减半而下降，方向正确）；冷启动同样呈现「首启冷缓存显著偏慢、其后 ~1s 热缓存」的模式。两次独立测量（不同日期、不同数据集规模）均落在 goals.md 候选目标的可达范围内。

## SLO 候选（全部未冻结）

以下候选只用于锚定后续测量，**不构成产品承诺**（goals.md：未冻结前不得宣称达标）：

| 指标 | goals.md 候选目标 | 本次测量 | 状态 |
|---|---|---|---|
| 冷启动 p95 | < 2 s | 1.78 s（首启冷缓存）/ 约 1.0 s（热） | 候选，需多机多轮复现 |
| 取消后停止反馈 | ≤ 2 s | 0.5–2.6 ms | 候选，量级远优于目标 |
| 扫描进度显示 | ≤ 1 s | 0.70–0.82 s（首进度回调） | 候选；UI 呈现时延未测 |
| 典型规则安全扫描 p95 | < 15 s | 3.42 s（本数据集口径） | 候选；「典型」数据集定义未冻结 |
| 空间分析 / 大文件 / 重复哈希 | 未设候选 | 见原始数据 | 仅基线，无目标 |

## 冻结条件（全部满足才可把候选改为「已冻结」）

1. goals.md 要求的基线定义齐备：设备、OS build、数据集构成、冷/热缓存、计时起止点、p50/p95、峰值内存、CPU 与磁盘读取量——本票已定义除**磁盘读取量**（未采集，待脚本补充）外的各项。
2. 至少两次相互独立的复跑（不同日期），结果落在同量级且可解释——✅ 已满足（2026-09-06 与 2026-09-08 两次运行，规模缩放方向正确；但两次的数据集规模不同，严格冻结仍需一次**同参数**的跨日复跑）。
3. 至少一台非开发机的复测，或明确声明 SLO 只对测量机有效——待满足。
4. 「典型规则安全扫描」的数据集构成获得确认（当前 2000 文件/595.6MB 是合成定义，不等于用户典型目录）。

**当前状态：所有候选仍未冻结。** 冻结条件 1（磁盘读取量）、2（严格同数据集跨日复跑）、3（非开发机复测）、4（典型数据集确认）尚有缺口。

## 已知局限

- 取消场景的首进度时延依赖真实规则集在合成数据上的命中节奏；规则库变化会让数字漂移。
- 冷启动样本仅 3–5 个且首次含文件缓存冷态，p95 统计意义有限。
- 未测量磁盘读取量、UI 端到端呈现时延与省电/降速行为。
- 单机数据不支持「全产品性能承诺」；任何对外表述必须引用冻结后的 SLO。
